# Fitness Tracker

A simple React + TypeScript + Vite app for logging workouts — track exercises with sets, reps, and weight, mark them complete, and persist your log locally.

## Getting started

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
